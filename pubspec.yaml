name: simple_widgets
description: "A new Flutter package project."
version: 0.0.1
homepage: https://github.com/koiralapankaj7/simple_widgets.git

environment:
  sdk: '>=3.2.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  simple_utils:
    path: ../simple_utils

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0

flutter:
